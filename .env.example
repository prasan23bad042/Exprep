# Frontend Environment Variables
# Copy this file to .env.local and update with your values

# API URL (optional - defaults to /api which uses proxy in development)
# VITE_API_URL=http://localhost:5000/api
